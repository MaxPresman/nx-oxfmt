{
  "name": "nx-oxfmt",
  "version": "0.1.0",
  "description": "Nx plugin for oxfmt - the high-performance Rust-powered code formatter",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "generators": "./generators.json",
  "executors": "./executors.json",
  "keywords": [
    "nx",
    "nx-plugin",
    "oxfmt",
    "formatter",
    "oxc"
  ],
  "license": "MIT",
  "dependencies": {
    "@nx/devkit": "^20.0.0",
    "tslib": "^2.6.0"
  },
  "peerDependencies": {
    "nx": ">=20.0.0",
    "oxfmt": ">=0.30.0"
  },
  "devDependencies": {
    "@nx/js": "^20.0.0",
    "@nx/plugin": "^20.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "npx eslint src/"
  }
}
